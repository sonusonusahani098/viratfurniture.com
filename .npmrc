# nuxt-og-image v6 optionally peers tailwindcss@4 while this project pins v3
# (required by @nuxtjs/tailwindcss). Safe to ignore — og-image only uses
# tailwind for optional class support in OG templates.
legacy-peer-deps=true
